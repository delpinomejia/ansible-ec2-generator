# ============================================================================
# Python Requirements for Ansible EC2 Generator
# ============================================================================
# Install with: pip install -r requirements.txt
# ============================================================================

# Core Ansible
ansible>=4.0.0,<8.0.0          # Ansible automation platform
ansible-core>=2.16.9           # Ansible core engine

# AWS Integration
boto3>=1.26.0                  # AWS SDK for Python
botocore>=1.29.0               # Low-level AWS service client library

# Additional Collections (install via ansible-galaxy)
# ansible-galaxy collection install amazon.aws
# ansible-galaxy collection install community.general

# Development and Testing (optional)
ansible-lint>=6.0.0            # Ansible best practices checker
yamllint>=1.28.0               # YAML linter
pytest>=7.0.0                  # Testing framework
pytest-ansible>=3.0.0          # Ansible testing plugin

# Security Scanning (optional)
bandit>=1.7.7                  # Security vulnerability scanner
safety>=2.0.0                  # Dependency vulnerability checker

# Documentation (optional)
mkdocs>=1.4.0                  # Documentation generator
mkdocs-material>=9.5.32         # Material theme for MkDocs

# Utility Libraries
requests>=2.32.4               # HTTP library
jinja2>=3.0.0                  # Template engine
pyyaml>=6.0.0                  # YAML parser
cryptography>=3.4.0            # Cryptographic library
authlib>=1.6.4 # not directly required, pinned by Snyk to avoid a vulnerability
setuptools>=78.1.1 # not directly required, pinned by Snyk to avoid a vulnerability
urllib3>=2.5.0 # not directly required, pinned by Snyk to avoid a vulnerability
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
marshmallow>=3.26.2 # not directly required, pinned by Snyk to avoid a vulnerability
